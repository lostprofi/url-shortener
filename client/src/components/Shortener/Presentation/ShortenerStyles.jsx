import { makeStyles } from '@material-ui/core/styles';

const fullURLForm = makeStyles({
  form: {
    width: '100%',
    marginTop: 50,
  },

  btn: {
    width: '100%',
    height: '100%',
  },

  input: {
    width: '100%',
  },

});

export default fullURLForm;
